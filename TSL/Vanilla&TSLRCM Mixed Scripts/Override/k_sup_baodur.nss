struct structtype1 {
	float float1;
	float float3;
};

void main() {
	vector vPosition = GetPosition(GetPartyLeader());
	float fFacing = GetFacing(GetPartyLeader());
	location location1;
	fFacing;
}

