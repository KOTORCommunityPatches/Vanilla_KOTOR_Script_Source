///increments the timer on tripped party init triggers

#include "k_inc_generic"
#include "k_inc_utility"
#include "k_inc_debug"

void main()
{
    //   string heartbeat = IntToString(GetGlobalNumber("G_Party_Init_Trig"));
    //    Db_PostString("Trig Count "+heartbeat);

  /*      if (UT_GetTalkedToBooleanFlag(OBJECT_SELF) == TRUE)
        {
        SetGlobalNumber("G_Party_Init_Trig",(GetGlobalNumber("G_Party_Init_Trig")+1));

        } */
}
